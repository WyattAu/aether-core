[package]
name = "stateful-actor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
aether-actor = { path = "../../crates/actor-sdk" }

[features]
default = ["aether-actor/std"]
